name: ai_core_os
description: AI Core OS - Framework for on-device AI - First real model run
publish_to: 'none'
version: 0.3.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  fllama: ^0.4.0  # For Qwen2.5-0.5B
  path_provider: ^2.1.2
  isar: ^3.1.0+1  # Prepared for v0.4.0, not used yet in v0.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/models/  # Place qwen2.5-0.5b.gguf here
