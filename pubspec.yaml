name: ai_core_os
description: AI Core OS v4 - Full Integrated OS
version: 4.0.0

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  isar: ^3.1.0+1
  isar_flutter_libs: ^3.1.0+1
  drift: ^2.28.0
  drift_flutter: ^0.2.4
  sqlite3_flutter_libs: ^0.5.20
  get_it: ^8.0.0
  fllama: ^0.0.1
  path_provider: ^2.1.3
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.13
  isar_generator: ^3.1.0+1
  drift_dev: ^2.28.0

# ده اللي بيحل المشكلة نهائيا
dependency_overrides:
  analyzer: ^6.9.0
  dart_style: ^3.0.0
